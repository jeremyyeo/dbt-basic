# prettier-ignore
models:
-   name: foo
    description: Something.
    config:
      access: public
      contract:
        enforced: false
    columns:
    -   name: concat(c, '#', d)
        description: something
        data_type: string
        data_tests:
        - unique
        - not_null
