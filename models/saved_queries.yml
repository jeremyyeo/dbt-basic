saved_queries:
  - name: my_saved_query
    config:
      cache:
        enabled: false
    query_params:
      metrics:
        - order_total
    exports:
      - name: my_export
        config:
          export_as: table
