models:
  - name: foo
    latest_version: 1
    config:
      contract: {enforced: true}
    columns:
      - name: id
        data_type: int
        constraints:
          - type: not_null
    versions:
     - v: 1
