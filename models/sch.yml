sources:
  - name: raw
    tables:
      - name: customers
        config:
          freshness:
            error_after:
              count: "{{ var('what') | int }}"
              period: hour
          loaded_at_field: updated_at
